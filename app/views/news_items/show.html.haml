%h3
  - if current_user.upvoted?(@news_item)
    "upvoted"
  - else
    = link_to('click here to upvote', news_item_upvote_path(@news_item))
  - if !@news_item.url.blank?
    = link_to(@news_item.title, @news_item.url)
  - else
    = @news_item.title
  - if !@news_item.text.blank?
    = @news_item.text
  posted by #{current_user.name} at #{@news_item.created_at.utc.strftime("%H:%M")} on #{@news_item.created_at.strftime("%m/%d/%y")}
    
- @news_item.comments.each do |c|
  = render :partial => 'comments/show.html', :locals => { :comment => c }

